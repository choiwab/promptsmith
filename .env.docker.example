# Optional: required only for POST /generate
OPENAI_API_KEY=

# Optional backend tuning
OPENAI_IMAGE_MODEL=gpt-image-1
OPENAI_VISION_MODEL=gpt-4.1-mini
APP_COMPARE_THRESHOLD=0.30

# Host ports
BACKEND_PORT=8000
FRONTEND_PORT=5173

# Frontend app title
VITE_APP_NAME=Promptsmith

# Seed deterministic demo data on backend startup
SEED_DEMO_DATA=true
